#!/bin/sh

fail() {
    echo $1 1>&2
    echo "Usage: bower ..." 1>&2
    exit 1
}

[ -f node_modules/.bin/bower ] && \
    fail "Impossible to found bower in the current folder."

node_modules/.bin/bower "$*"
