#!/bin/sh
set -e -u

# check bash

grep -e "^#\\!/bin/bash" -l -r "${PWD}" | grep -v "\\.git" | xargs shellcheck --shell bash

# check shell

grep -e "^#\\!/bin/sh" -l -r "${PWD}" | grep -v "\\.git" | xargs shellcheck --shell sh
