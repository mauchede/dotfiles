[Install]
WantedBy=default.target

[Service]
EnvironmentFile=%h/.env
Environment="IMAGE=selenium/standalone-firefox:latest"
Environment="NAME=selenium-firefox-%u"
ExecStart=/usr/local/sbin/docker run --init --name "${NAME}" --rm "${IMAGE}"
TimeoutStartSec=0
