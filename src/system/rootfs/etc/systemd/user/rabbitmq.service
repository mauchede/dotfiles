[Install]
WantedBy=default.target

[Service]
EnvironmentFile=%h/.env
Environment="IMAGE=rabbitmq:management"
Environment="NAME=rabbitmq-%u"
ExecStart=/usr/local/sbin/docker run --init --name "${NAME}" --rm "${IMAGE}"
TimeoutStartSec=0
